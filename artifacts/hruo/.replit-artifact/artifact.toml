kind = "web"
previewPath = "/"
title = "HRUO - Enterprise HCM System"
version = "1.0.0"
id = "artifacts/hruo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/hruo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hruo", "run", "build" ]
publicDir = "artifacts/hruo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
